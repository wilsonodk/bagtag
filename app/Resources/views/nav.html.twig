{# app/Resources/views/nav.html.twig #}
<nav class="navbar navbar-default">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('homepage') }}"><span class="glyphicon glyphicon-tag" aria-hidden="true"></span> Bag Tags</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li {% if route == 'stores' or route == 'add_store' or route == 'store_by_id' or route == 'edit_store_by_id' %}class="active"{% endif %}><a href="{{ path('stores') }}">Stores</a></li>
                <li {% if route == 'players' or route == 'add_player' or route == 'player_by_id' or route == 'edit_player_by_id' %}class="active"{% endif %}><a href="{{ path('players') }}">Players</a></li>
                <li {% if route == 'tournaments' or route == 'add_tournament' or route == 'tournament_by_id' %}class="active"{% endif %}><a href="{{ path('tournaments') }}">Tournaments</a></li>
            </ul>
        </div><!--/.navbar-collapse -->
    </div>
</nav>
